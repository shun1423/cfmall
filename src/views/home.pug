extends base
include mixins/video 

block content 
    div.video-grid
        each info in videos 
            +video(info)
            

        else 
            span.empty__message Product not found